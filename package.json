{
  "dependencies": {
    "@stablelib/base64": "^1.0.1",
    "react": "^19.2.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@types/node": "26.0.0",
    "@vitest/coverage-istanbul": "4.1.6",
    "@vitest/coverage-v8": "^4.1.5",
    "@vitest/ui": "4.1.6",
    "drizzle-orm": "^0.45.2",
    "turbo": "^2.9.12",
    "typescript": "6.0.3",
    "vitest": "4.1.6"
  },
  "engines": {
    "node": ">=18"
  },
  "name": "winery",
  "packageManager": "bun@1.3.11",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "check": "biome check --write .",
    "check-types": "turbo run check-types",
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=web",
    "dev:server": "turbo run dev --filter=server",
    "format": "biome format --write .",
    "generate": "turbo run generate",
    "lint": "biome lint .",
    "db:generate": "turbo run db:generate --filter=server",
    "db:migrate": "turbo run db:migrate --filter=server",
    "db:push": "turbo run db:push --filter=server",
    "db:seed": "turbo run db:seed --filter=server",
    "db:seed:demo": "turbo run db:seed:demo --filter=server",
    "db:studio": "turbo run db:studio --filter=server",
    "test": "turbo run test -- --run",
    "test:coverage": "turbo run test:coverage -- --run",
    "test:e2e": "turbo run test:e2e",
    "validate": "echo 'Running mandatory pre-push validation...' && bun run lint && bun run check && bun run generate && bun run build && bun run check-types && bun run test && bun run test:e2e"
  },
  "workspaces": [
    "apps/*",
    "packages/shared",
    "packages/typescript-config"
  ]
}
