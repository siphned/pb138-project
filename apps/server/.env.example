DATABASE_URL=postgresql://postgres:postgres@localhost:5432/winemarket

CLERK_SECRET_KEY=sk_test_...
CLERK_JWT_KEY="-----BEGIN PUBLIC KEY-----
...
-----END PUBLIC KEY-----"

FRONTEND_URL=http://localhost:5173

# Email (Resend). Without these, order + role-request emails fail silently.
# FROM_EMAIL must be a Resend-verified domain, or use onboarding@resend.dev for testing
# (test mode can only deliver to the Resend account owner's address).
RESEND_API_KEY=re_...
FROM_EMAIL=onboarding@resend.dev

TEST_USER_CLERK_ID=user_test_user_placeholder
